# /etc/fail2ban/filter.d/otrs-mcp-login.conf
#
# Detecta tentativas de login falhadas no endpoint /api/admin/login
# Captura respostas 401 (credenciais invalidas) e 429 (rate limit)

[Definition]
failregex = ^<HOST> .* "POST /api/admin/login HTTP/.*" (401|429)
ignoreregex =
