# /etc/fail2ban/filter.d/otrs-mcp-api.conf
#
# Detecta abuso da API: muitas requests sem autorizacao (401)
# ou tentativas de acesso indevido (403)

[Definition]
failregex = ^<HOST> .* "(?:GET|POST|PUT|DELETE|PATCH) /(?:api|mcp)/.* HTTP/.*" (401|403)
ignoreregex = ^<HOST> .* "POST /api/admin/login
